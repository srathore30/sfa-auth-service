spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pro_product
    username: root
    password: 14640548root

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  spring:
    security:
      enabled: false

  application:
    name: auth-service

server:
  port: 9095
  servlet:
    context-path: /auth-service

application:
  config:
    trace: true

jwt:
  tokenValidityInSeconds: 720000 #2 hours